package inflearn_study_algorithm;

import java.util.Scanner;
import java.util.Stack;

import javax.naming.spi.DirStateFactory.Result;
import javax.swing.plaf.basic.BasicInternalFrameTitlePane.IconifyAction;

public class Stack_Queue_02 {

	public static void main(String[] args) {
		
		/*
		* ABOUT
		*
		* DATE: 2022-10-25
		* AUTHOR: dms873
		*
		* TITLE: 괄호 문자 제거      
		*
		* COMMENT: 
		* 
		**/
		
		Scanner sc = new Scanner(System.in);
		// System.out.print("문자열 입력 : ");
		String str = sc.nextLine();
		sc.close();
		
		Stack<Character> st = new Stack<>();
		for(int i = 0; i < str.length(); i++) {
			if(str.charAt(i) != ')') st.add(str.charAt(i));
			else {
				while(st.contains('(')) {
					if(st.indexOf('(') < st.lastIndexOf('(')) {
						st.pop();
						if(st.indexOf('(') == st.lastIndexOf('(')) break;
					}
					st.pop();
					System.out.println("indexOf : " + st.indexOf('('));
					System.out.println("lastIndexOf : " + st.lastIndexOf('('));
					System.out.println("ccc : " + st.contains('('));
					System.out.println("st : " + st);
				}
			}
			System.out.println("st2 : " + st);
		}
		String result = "";
		for(int i = 0; i < st.size(); i++) result += st.elementAt(i);
		System.out.println(result);
	}
}
